import { StyleSheet, Text, FlatList } from 'react-native';

const Feed = () => {
    return (
        <FlatList>
            <Text>Feed</Text>
        </FlatList >
    )
}

export default Feed;

const styles = StyleSheet.create({})